<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.ac.iiitd.prankul.testhimmt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StudentQueryActivity"
            android:label="@string/title_activity_student_query"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StudentFollowUpQueryActivity" />
        <activity android:name=".StartNewStudentQueryActivity" />
        <activity
            android:name=".TaQueryActivity"
            android:label="@string/title_activity_ta_query"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartNewTaQueryActivity" />
        <activity android:name=".TaFollowUpQueryActivity"></activity>
    </application>

</manifest>